# (INSTALLATION)

#pkg update && pkg upgrade

#pkg install git python

#pip install requests pyfiglet 

#colorama

#git clone git@github.com:Termuxstar/zabi_checker.git

#cd zabi_checker

#(nano zabi_checker.py) type this and add api_key = 'YOUR_GOOGLE_API_KEY'  replace it and enter your google safe browsing api 

#python zabi_checker.py
